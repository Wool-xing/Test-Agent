# Test-Agent QA 验收报告（基线）

日期：2026-08-16 · 模式：全维度验收（pre-release-qa + 5 审查军团交叉验证）· **本轮为基线（无历史对比）**

## 汇总

**问题总数：45**（6 CRITICAL / 14 HIGH / 15 MEDIUM / 10 LOW）— 全部经人工复核（CRITICAL 逐条读码确认）

| 维度 | 结果 | 说明 |
|---|---|---|
| 基线测试（真实跑） | ⚠ 部分 | pytest **1145 passed / 4 failed**；覆盖率 **41%**（规则 80%）；E2E 测试缺 Chromium ⏸ |
| CLI 功能 | ✅ | 11 命令 --help 全过；catalog/doctor/plan/init 实跑通过；catalog 1.2s，doctor 1.1s |
| API（真实 curl/httpx） | ⚠ | 27 路由；run/text 全链路 9/9 节点成功；`/status/{unknown}` 伪造 "running" 200；认证默认关闭 |
| selftest | ⚠ | `--e2e` stub 7/7 通过；无参调用 = no-op（与文档 L1-L4 不符） |
| 代码审查（python） | ❌ | 2C/4H/6M/3L |
| 静默失败审查 | ❌ | 1C/4H/2M |
| 安全审查 | ❌ | 3C/2H/3M/4L（bandit 2187 条，HIGH 6 条复核后 3 条误报） |
| 性能审查 | ⚠ | 0C/3H/3M/3L；CLI 冷启动 0.6s，catalog 1.2s 健康 |
| TypeScript（desktop-v2） | ❌ | 1C（CSP 阻断 IPC，App 永久 Offline）/3M/4L；tsc --noEmit 通过 |
| CI | ❌ | main 红：ci.yml pytest-unit 3 MCP 失败（08-03 起）；daily-audit 红：要求 gitignored 文件 |
| 硬编码 | ✅ | runtime/utils 无绝对路径硬编码、无硬编码密钥；git 历史无泄漏密钥 |
| 弱网/soak/压测 | ⏸ 未跑 | 需真实环境 |
| 真 LLM（L3/L4） | ⏸ 未跑 | 需真实费用（~$3/次）；CI 周自检 job 覆盖 |

## CRITICAL（P0，立即处理）

| # | 位置 | 问题 | 验证 |
|---|---|---|---|
| 1 | runtime/cli/commands/run.py:54-58 | `tagent run` 失败时不 `typer.Exit(1)` — CI 用 tagent run 做门禁时测试失败也绿 | 读码确认 |
| 2 | runtime/orchestrator/flows.py:112-119 | 熔断 break 后未执行节点不记入 failures/skipped，`succeeded = len(ordered) - failed - skipped` 把没跑的节点算通过。direct.py 兜底路径正确，双执行器结论不一致 | 读码确认 |
| 3 | runtime/api/endpoints/webhooks.py:341-362 | 飞书 webhook 零签名校验 — 任意 POST 驱动完整 LLM 流水线（成本消耗 + 提示注入） | 读码确认 |
| 4 | runtime/config/settings.py:64 + docker-compose.app.yml:20 | `api_auth_token` 默认空 = 认证默认关闭，8800 端口对外 — 默认配置部署即未授权 API | 读码确认 |
| 5 | runtime/orchestrator/adapters/experts.py:172-182 | `passed = len(bug_list)`、`pass_rate = 1.0` 硬编码 — 有 P0 bug 也报 100% 通过 | 读码确认 |
| 6 | apps/desktop-v2/tauri.conf.json:24 | CSP `connect-src` 缺 `ipc:` / `http://ipc.localhost` — `invoke()` 全平台被拦，App 永远显示 "Offline" | TS reviewer + tsc 通过但运行时必断 |

## HIGH（P1）

| # | 位置 | 问题 |
|---|---|---|
| 7 | CI（ci.yml pytest-unit） | main 红：test_mcp_client 3 失败（MCPError Connection closed / assert 0>=5），08-03 起未修 |
| 8 | .github/workflows/daily-audit.yml:294 | 门禁要求 gitignored 的 workspace/INDEX.md — 每个全新 checkout 必红 |
| 9 | runtime/storage（tagent.db） | **零张表**：无 create_all/alembic 执行；run 状态持久化静默失败（`no such table: runs`）；doctor 报 "DB OK" 为假阳性 |
| 10 | runtime/api（/status/{run_id}） | 不存在的 run_id 返回伪造 `"status":"running"` HTTP 200 |
| 11 | pyproject.toml dependencies | `pip install test-agent` 后 CLI 崩溃（sqlalchemy 等缺失，依赖散在 requirements/base.txt，双清单漂移） |
| 12 | 覆盖率 41% | 低于 80% 规则；CI 注释声称 `--cov-fail-under=30` 但命令未带该 flag，门禁未生效 |
| 13 | runtime/api/auth/sso.py:170-217 | `validate_token` fail-open：无 exp 接受、alg 取攻击者头、未预热 JWKS 即放行 |
| 14 | runtime/api/middleware/enterprise.py + main.py:57-75 | 整套企业认证（SSO/RBAC/AuditTrail）从未挂载；EnterpriseMiddleware 无 token 时以匿名 VIEWER 放行 |
| 15 | runtime/api/endpoints/webhooks.py:258-276 | telegram webhook 无签名校验（其余平台有） |
| 16 | runtime/config/settings.py:74 | 文档宣称的 pentest 法律门禁 `pentest_authorized` 零读取者 — 门禁不存在 |
| 17 | utils/data/db_test_helper_v2.py:115-161 | FK/唯一/check 约束测试把**任何**异常当"约束生效"passed=True — DB 挂了也报 PASS |
| 18 | utils/testing/state_machine_tester_v2.py:71-72 | `except Exception: pass` — 转换动作抛异常仍记 executed |
| 19 | utils/testing/differential_tester.py:165-170 | 两边 200 但 JSON 解析失败 → 静默 identical=True |
| 20 | runtime/router/v2_prompt.py:158 | 每次 LLM 路由请求 json.loads 5MB graph.json + O(N×words) 扫描，无缓存 |
| 21 | runtime/intelligence/impact_engine.py:163 | 每测试节点独立 reverse-BFS，O(T×(V+E)) |
| 22 | runtime/cli/search.py:56-66 | 每条消息开新 sqlite 连接 + DDL + commit，1000 消息 = 1000 次 |

## MEDIUM（P2）

| # | 位置 | 问题 |
|---|---|---|
| 23 | requirements/*.txt | pytest-asyncio 不在任何 requirements（CI 手工装）— 本地 async 测试静默失效 |
| 24 | runtime/pyproject.toml:82 | 文档 21 个测试 marker（@unit/@fuzzing/@a11y…）实际注册 4 个 |
| 25 | utils/a11y_i18n/i18n_checker.py:331-365 | `run_taboo_audit({})` 按当天日期出 hit（08-16 total_hits=1）→ test_utils_i18n_taboo 日期依赖失败 |
| 26 | runtime/tests/test_basic_skills.py | test_ping_script_cli 全量跑偶发失败、单跑 3/3 过 — order/网络依赖 flaky |
| 27 | runtime/orchestrator/flows.py | Prefect cache_key_fn 无法 hash `_thread.lock` → 每次 DAG 报 ValueError（可恢复但吵） |
| 28 | runtime/cli/commands/selftest.py:23-24 | 无参 selftest = no-op，与文档 L1/L2/L3/L4 分层不符 |
| 29 | ruff（根 scope） | 525 lint 错误 / 368 文件未 format；CI 只查 utils/ + specs/，根 scope 从未治理 |
| 30 | runtime/sdk/publish.py:20-32 | tar 校验不查 symlink/hardlink member（3.10/3.11 无 filter="data" 保护） |
| 31 | runtime/cli/_shared.py | LazyKernel 冷启动修复未提交（工作树脏：CLAUDE.md、_shared.py、SPRINT-STATUS 等） |
| 32 | runtime/api/main.py:82 | /api/marketplace 豁免 token 认证且无内部认证 |
| 33 | runtime/config/settings.py:70 | max_concurrent_runs 从未强制（仅 TUI 展示）— API 后台任务无上限 |
| 34 | runtime/orchestrator/adapters/experts.py:312 | runner.run() 无 timeout — LLM 挂起卡死 DAG |
| 35 | runtime/api/main.py:133-137 | set_mode/set_lang 改模块全局 — 并发 /run/* 请求竞态 |
| 36 | runtime/api/main.py:155 | UploadFile 50MB 上限无效（File(max_length) 不作用于 UploadFile） |
| 37 | utils/security/api_security_scanner_v2.py:78,155 | bare except continue — 目标全挂时"零发现"与"干净"无法区分 |
| 38 | runtime/learning_loop/session_search.py:52-87 | 每次调用执行 DDL + 新连接 |
| 39 | runtime/learning/session_store.py:213-229 | get_stats 全量载入 json.loads，无界增长 |
| 40 | runtime/web/kg_api.py:166-182 | `queue.pop(0)` O(n²) BFS + 每请求 3 次全扫描 |

## LOW（P3）

| # | 位置 | 问题 |
|---|---|---|
| 41 | CLAUDE.md | "38 skills" vs registry 37（README.md 被算进去） |
| 42 | daily-audit.yml:290 | awk 把 wc 的 `total` 行当 >800 行文件 → 永久假 WARN |
| 43 | bandit 误报 | WeChat SHA1（协议强制）、iot AutoAddPolicy（flag 门控+默认 Reject）、fuzzer BIDI（测试载荷） |
| 44 | runtime/api/auth/rbac.py:142-149 | 未知 role 静默降级 viewer 而非拒绝 |
| 45 | 杂项 | flows.py:98 for-else 死分支；mcp decisions 日志存全量 kwargs（可能含密钥）；llm_cache 永不过期；main.rs 硬编码 "2.0.0"；.env.bak 未 gitignore；Actions Node 20 弃用警告 |

## 基线对比

无历史报告 — 本轮即基线。下轮对照本文档。

## P0 修复后回归（2026-08-16）

- 全量套件：**1152 passed / 7 failed / 1 error**（基线 1145 passed / 4 failed；+7 为新测试）
- 持久失败（与 P0 修复无关）：
  - test_sprint5_e2e ×2 — 本地无 Chromium（⏸ 环境，CI 绿）
  - test_utils_i18n_taboo::test_minimal_payload — 日期依赖（P2#25）
- **新增发现：flaky 集群** — test_basic_skills（ping/http/timeout/file 4 例）+ test_skill_sdk::test_publish_to_local_registry 在全量跑时随机失败、单跑全过（order/环境依赖）。基线跑已有 ping 例，本轮扩大。建议 P2 队列加入 flaky 隔离（项目自带 utils/flaky_detector.py）。
- 受影响模块单测：cli_commands 6/6、orchestrator_core 62/62、webhooks 49/49、agent_runners 38/38、api_auth+marketplace 11/11 全绿

## 盲点自查（本轮未覆盖）

1. **Flutter mobile（apps/mobile）**未审查 — 无 flutter reviewer 名额
2. **Electron 桌面（apps/desktop）**未审查 — 只查了 desktop-v2
3. **弱网/soak/混沌/压测** ⏸ — 无真实环境
4. **真 LLM 路径** ⏸ — L3 selftest ~$3/次；test_router_real 未跑（CI 周自检 job 覆盖，上周绿）
5. **E2E 浏览器测试** ⏸ — 本地未装 Chromium（playwright install 未跑）；CI 通过
6. **Python 3.10/3.12** 运行时未验证（CI 仅 3.11；本地 3.14 通过）
7. macOS 本地未测（CI install.py macOS e2e job 绿）
8. **prompt_guard.sanitize_input 是否接入 /run/text** — 审查者未能确认调用链
9. **JMeter/perf.txt 压测套件**仅 locust 声明，无实际压测脚本运行
10. **sdk/、engine/（Rust）、specs/gates** 深度未审（CI 有 mypy + rust check 绿）

## 修复进度（P0→P3，逐一修）

| # | 状态 | 修复 | 验证 |
|---|---|---|---|
| P0-1 | ✅ 已修 | run.py:54-61 失败时 `typer.Exit(1)` | 新测试 test_run_exits_nonzero_when_tests_fail，6/6 过 |
| P0-2 | ✅ 已修 | flows.py `_mark_unrun_nodes` 熔断后未执行节点计入 skipped | 新测试 TestMarkUnrunNodes，62/62 过 |
| P0-3 | ✅ 已修 | feishu `_verify_feishu` 验证 token，fail-closed；.env.example 补文档 | 新测试 TestFeishuSignatureVerification，49/49 过 |
| P0-4 | ✅ 已修（用户决策：保持现状） | docker-compose.app.yml 绑定 127.0.0.1 + 空 token 启动告警；认证保持 opt-in（本地工具 UX），公网部署自设 token | 11/11 过 |
| P0-5 | ✅ 已修 | experts.py 移除伪造 passed/pass_rate=1.0，P0/P1 bug → 不通过 | 新测试 TestReportSummaryTruthfulness，38/38 过 |
| P0-6 | ✅ 已修 | tauri.conf.json CSP 补 `ipc:` + `http://ipc.localhost` | vite build ✅ |
| P0-7 | ✅ 已修 | **sec CRITICAL 3**：Discord/QQ/DingTalk 校验在 TAGENT_ENV=dev 时无条件返回 True — 去 dev 绕过，fail-closed | 三处 dev 绕过删除；QQ op=0/13 强制签名（心跳 ACK 无载荷豁免）；新测试 test_no_public_key_rejected_even_in_dev ×3 等，webhooks 51/51 过 |
| P1-7 | ✅ 已修 | CI main 红：test_mcp_client 3 失败（mcp 2.0.0 删 Server.list_tools 装饰器） | ci.yml + pyproject pin `mcp<2.0.0`（→1.29.0）；PR #441 pytest-unit SUCCESS；连带修 test_utils_i18n_taboo 日期依赖（pin 02-03）+ test_skill_sdk mkdtemp 下划线名（固定名）|
| P1-8 | ✅ 已修 | daily-audit.yml 关键文档门禁要求 gitignored 的 workspace/INDEX.md（从未生成，每 checkout 必红） | 门禁列表删 `workspace/INDEX.md`；本地复跑循环 6/6 OK；yaml 解析 8 jobs OK |
| P1-9 | ✅ 已修 | tagent.db 零张表：无 create_all/alembic 执行；run 持久化静默失败（no such table: runs）；doctor "DB OK" 假阳性（只 SELECT 1） | db.py get_engine 首建引擎时初始化 schema（每进程一次）：sqlite → create_all，pg → alembic upgrade head（pgvector ext + vector 列）；doctor ping_db 改 inspect 真表检查（6 表缺一报红）。验证：全新 sqlite 6 表自动建、create_run/set_run_status 持久化 OK、doctor 报 "DB OK (6 tables)"、104 测试绿 |
| P1-10 | ✅ 已修 | status 未知 run_id 伪造 "running" 200 → 404；`_run_active` 集合区分在途/不存在（含瞬态竞态防护） | 新测试 test_api_status.py 3/3 过；api_auth+marketplace 回归 14/14 绿 |
| P1-11 | ✅ 已修 | 根 pyproject base 依赖同步 runtime 完整清单（litellm/prefect/sqlalchemy/fastapi 等）；`all` 只留 playwright/requests | 干净 venv `pip install .` 后 doctor/catalog/run/serve 全部无 ImportError；doctor "DB OK (6 tables)" |
| P1-12 | ✅ 已修 | ci.yml 补 `--cov-fail-under=30`（注释声称有、实际没有） | 本地全量 1157 passed：门禁判定 "Required coverage of 30% reached (41.29%)"。附带修 test_api_auth.py 三处 `_settings.api_auth_token` 泄漏（monkeypatch.setattr），api 测试簇 65/65 绿 |
| P1-13 | ✅ 已修 | sso.py fail-open 三处：缺 exp 拒绝、alg 白名单（none/HS 拒绝）、无 JWKS fail-closed 拒绝；async 路径同修 + jwt.PyJWTError → 401 | 3 新测试 + 改 1 旧断言（原断言 fail-open 行为）；enterprise+sprint4/6+permissions 65/65 绿 |
| P1-16 | ✅ 已修 | pentest 法律门禁零读取者 — base.py run() 加 `pentest-*` 前缀门禁（TAGENT_PENTEST_AUTHORIZED=1） | 新测试 TestPentestLegalGate 3/3；agent_runners+skill_runners 96/96 绿 |
| P1-17 | ✅ 已修 | db_test_helper_v2 三函数 `except Exception` → "约束生效" 误报 — 只认 IntegrityError | 新测试 test_db_test_helper_v2.py 5/5（sqlite 真库正路径 + 缺表负路径） |
| P1-18 | ✅ 已修 | state_machine_tester_v2 execute_action 吞异常 — 异常传播；apply 拒绝转换返回 None；walk 记录 action_error | 新测试 test_state_machine_tester_v2.py 5/5 |
| P1-19 | ✅ 已修 | differential_tester 两边 200 + JSON 解析失败 → 静默 identical=True — 改记 divergence_type=json_parse_error | 新测试 test_differential_tester.py 3/3（真 HTTP server 线程） |
| P1-14 | ✅ 已修 | EnterpriseMiddleware 从未挂载 + 无 token 匿名 VIEWER 放行 — ① middleware 无 token 401 fail-closed ② main.py TAGENT_ENTERPRISE_ENABLED=1 opt-in 挂载，SSO 配置不全拒绝启动 | 新测试 TestEnterpriseMiddleware 3/3；子进程验证 mounted=True / RuntimeError 两路径 |
| P1-15 | ✅ 已修 | telegram webhook 零签名 — X-Telegram-Bot-Api-Secret-Token 校验，未配置 fail-closed；.env.example 补文档 | 新测试 5/5（含 missing/wrong/unconfigured 401） |
| P1-20 | ✅ 已修 | v2_prompt 每请求 json.loads 5MB graph.json — mtime 失效模块级缓存 | 新测试 test_v2_prompt_kg.py 3/3 + v2_router 33/33 |
| P1-21 | ✅ 已修 | impact_engine 每测试节点独立 reverse-BFS O(T×(V+E)) — adj_reverse 是精确逆，循环冗余，删除 → O(V+E) | test_impact_engine 16/16 行为不变 |
| P1-22 | ✅ 已修 | search.py index_session 每消息开连接+DDL+commit — 一连接一事务批量化 | 新测试 test_cli_search.py 3/3（_ensure_db 计数=1） |

### 第二轮审查军团发现（2026-08-16，3 agents 并行审未提交 diff）

| # | 严重度 | 位置 | 问题 | 处置 |
|---|---|---|---|---|
| R1 | HIGH(误报) | flows.py:113 | "_mark_unrun_nodes 只在 cancelled 时跑" — 实际无条件调用，未消费节点必进 skipped | ✅ 验证后驳回（已有 TestMarkUnrunNodes 覆盖） |
| R2 | MEDIUM→已修 | experts.py:184-187 | degraded 块覆盖 "不通过" verdict — P0 bug + degraded agent = 有条件通过 | ✅ 已修：verdict != 不通过 才降级；新测试 2/2 |
| R3 | MEDIUM→已修 | db.py:26 | alembic 多进程竞态 — pg_advisory_lock + NullPool 专用连接序列化；alembic url 指向运行时配置 | ✅ 已修：sqlite 回归 29/29；PG 路径无本地环境，读码级验证 |
| R4 | HIGH→已修 | webhooks.py:200-212 | DingTalk 重放 — 5 分钟窗口（ms） | ✅ 已修：webhooks 57/57（含新重放测试） |
| R5 | MEDIUM→已修 | webhooks.py:154-159 | WeChat `==` 时序 + 重放 — compare_digest + 5 分钟窗口 | ✅ 已修：webhooks 57/57（含新 stale 测试） |
| R6 | MEDIUM→已修 | webhooks.py:253-268 | Telegram 零签名（= P1-15） | ✅ 已修 |
| R7 | MEDIUM→已修 | sso.py:268 | get_user_info 返回未验签 claims — 502 fail-closed | ✅ 已修：enterprise 42/42 |
| R8 | LOW→已修 | sso.py:183 | exp 非数字 TypeError 500 — isinstance 检查 401 | ✅ 已修 |
| R9 | LOW | main.py:98 | _run_active 无上限（execute_sync 挂起时增长） | ⏳ 记录（挂起跑 = 运行中语义，行为与原一致；P2#34 timeout 修后自然收敛） |
| R10 | LOW→已修 | pyproject.toml | otel-instrumentation-fastapi 预发布无上限；litellm 无上限 — 双清单 pin（litellm<2.0.0、otel<0.66b0） | ✅ 已修 |
| R11 | LOW | _shared.py:17-28 | _LazyKernel 双线程初始化竞态（单线程路径良性） | ⏳ 记录 |
| R12 | LOW | webhooks.py:339-346 | _verify_feishu 要求每次回调带 token 字段（v2 事件可能不带） | ⏳ 待确认官方文档 |

### 第二轮基线

- pytest 全量：**1211 passed / 3 failed**（最终回归；上轮 1152/7；剩 3 = 已知：flaky ping（rerun 机制已接管）、无 Chromium e2e ×2）
- 覆盖率门禁：41.29% ≥ 30 ✅ 生效
- `tagent selftest` 无参 L2 实跑：9/9 节点 PASS（修复前为 no-op 假绿）

### P2 修复进度（2026-08-16 07:15~07:42）

| # | 状态 | 修复 | 验证 |
|---|---|---|---|
| P2-23 | ✅ | requirements/dev.txt 补 pytest-asyncio（CI 手工装 → 清单化） | 配置级 |
| P2-24 | ✅ | runtime/pyproject markers 4 → 16（按配置清单.md 补 unit/fuzzing/a11y/api/ui/regression/performance/security/p0~p3） | 配置级；实际代码只用已注册 4+内置 |
| P2-25 | ✅ | i18n_checker run_taboo_audit 根修：无 date 不扫节假日（消除日期依赖） | 新测试 test_empty_payload_date_independent；i18n 55/55 |
| P2-26 | ✅ | flaky 集群隔离：4 类（ping/http/file/timeout）+ skill_sdk publish 打 @pytest.mark.flaky | `-m "not flaky"` 13 deselected 生效；全量 26/26 |
| P2-27 | ✅ | ExecutionContext 的 Lock 字段致 Prefect hash _thread.lock ValueError — 锁移模块级共享 | cloudpickle 125B ✓；orchestrator_core+workflow_gates 77/77 |
| P2-28 | ✅ | selftest 无参 no-op → L2 stub LLM 端到端（settings 实例改 provider，因单例不重读 env） | `tagent selftest` 实跑 9/9 PASS |
| P2-30 | ✅ | sdk/publish tar 校验补 symlink/hardlink 拒绝（3.10/3.11 回退路径 zip-slip） | 新测试 test_publish_symlink_member_rejected；skill_sdk 13/13 |
| P2-34 | ✅ | Responses API 路径 openai client 默认 600s 无界 → 用 llm_timeout_seconds（litellm 路径本有 60s） | 新测试 TestResponsesApiTimeout；model_router 20/20 |
| P2-37 | ✅ | api_security_scanner_v2 两处 bare except → 全失败时显式 WARN "unable to scan" | 新测试 2/2（requests 全挂模拟） |
| P2-29 | ⏳ | ruff 根 scope 525 lint 错误 — 独立战役 | 排队 |
| P2-31 | ✅(在树) | LazyKernel 冷启动修复已随工作树提交 | - |
| P2-32 | ✅ | marketplace token 豁免移除 — token 设置时全端点需认证（桌面端只静态文案不调 API） | 新测试 401；api 测试簇 17/17 |
| P2-33 | ✅ | max_concurrent_runs 强制 — _check_concurrency 429 | 新测试 429；17/17 |
| P2-35 | ✅ | run_text set_mode/set_lang 模块全局竞态 — _mode_lang_lock 临界区 | 11/11 |
| P2-36 | ✅ | UploadFile 50MB 上限端点内强制（413）— 附带发现：File(max_length) 对 UploadFile 致 pydantic TypeError，/run/file 任何上传本即 500，已一并修 | 新测试 413；17/17 |
| P2-38 | ✅ | session_search 每次调用 DDL — _initialized 一次性标志 | 新测试 test_session_search.py 3/3 |
| P2-39 | ✅ | session_store.get_stats 全量 json.loads — SQL json_extract 聚合 | test_session_store 8/8 |
| P2-40 | ✅ | kg_api pop(0) O(n²) → deque + _resolve_node_id 3 次全扫描 → 1 次 | test_kg_api 7/7 |
| P3-41 | ✅ | CLAUDE.md 38→37 skills（README.md 误计） | - |
| P3-42 | ✅ | daily-audit awk total 行当文件 — 过滤 $2!="total" | 本地复跑只报真 >800 文件 |
| P3-44 | ✅ | rbac require 未知 user_roles 静默降级 viewer — 非空未知列表 403 | 新测试 3/3；enterprise 45/45 |
| P3-45 | 部分 | flows.py for-else 死分支已删；mcp kwargs 日志/llm_cache/main.rs 硬编码/.env.bak/Actions Node20 留待 | - |

### QA 第三轮（2026-08-16 08:00~，3 agents 审第二轮 diff）

基线：1211 passed / 3 failed 稳定。盲点 #8 关闭（prompt_guard.sanitize_input 接线 /run/text，此前零生产调用）。

| # | 严重度 | 来源 | 问题 | 处置 |
|---|---|---|---|---|
| S1 | CRITICAL | sec | sso `_check_claims` 从 claims 读 alg — alg 在 JOSE header，所有 token 恒 401（SSO 彻底不可用；测试 vacuous 只断言 401） | ✅ 已修：`_header_alg` + 显式传参；新测试断言到达 JWKS 检查（detail 含 "JWKS"）；enterprise 46/46 |
| S2 | HIGH | sec+code | 429 限流 check/add 分离锁 TOCTOU — 超配过 max | ✅ 已修：check+add 同一 `_run_lock` 持有（3 端点）；18/18 |
| S3 | HIGH | sec | 413 检查在 `file.read()` 全量缓冲之后 — 流式多 GB 内存+磁盘 DoS | ✅ 已修：1MB 分块累加上限；6/6 |
| S4 | MEDIUM | sec | compare_digest 非 ASCII str → TypeError 500（telegram/dingtalk/wechat/feishu 四处） | ✅ 已修：全部转 bytes；webhooks 58/58 |
| S5 | MEDIUM | sec | publish 3.10/3.11 回退接受 FIFO/device 成员（阻塞 open DoS） | ✅ 已修：isfifo/ischr/isblk 拒绝 + 测试；skill_sdk 14/14 |
| S6 | MEDIUM | code | enterprise 模式 401 所有 webhook 回调（平台无法带 SSO token） | ✅ 已修：mount exclude_paths 加 6 条 webhook 路径 |
| S7 | MEDIUM | code | alembic url `%` 转义 — 含 %40 密码的 DATABASE_URL 必崩 | ✅ 已修：`url.replace("%","%%")`（读码级，无本地 PG） |
| S8 | MEDIUM | code | conversation.dump() 逐条 index_message（200 消息 = 200 连接+DDL） | ✅ 已修：改批处理 index_session；18/18 |
| S9 | LOW | code | index_session 单行坏数据拖垮整批 | ✅ 已修：per-row try/except；75/75 |
| S10 | LOW | code | feishu encrypt 模式无 token 字段 → 恒 403 | ✅ 已修：`_decrypt_feishu`（AES-256-CBC, key=SHA256）+ PKCS7；58/58 |
| S11 | LOW | sec | roles 子串匹配（"administrator" 字符串 claim → ADMIN） | ✅ 已修：isinstance list 守卫；enterprise 67/67 |
| S12 | MEDIUM | sec | enterprise 认证了但无授权 — 无端点用 @rbac.require | ⏳ 排队（端点×权限矩阵 = 产品决策） |
| S13 | LOW | sec | 401 detail 区分 iss/exp/aud/alg — 探测可学配置 | ⏳ 记录（与调试便利权衡） |

### QA 第四轮（2026-08-16 08:40~，盲点关闭轮）

基线：1215 passed / 3 failed 稳定（v2_prompt_kg mtime flake 已用显式 utime 修）。Gemini 交叉验证 ⏸（GEMINI_API_KEY 未配）。

**Flutter mobile（首轮盲点，flutter-reviewer）**

| # | 级别 | 问题 | 处置 |
|---|---|---|---|
| F1 | CRITICAL | 占位证书 pin 无效 base64 — Android 解析崩溃 / iOS 拒全部 TLS | ✅ 已修：移除占位 pin（系统信任+禁 cleartext），注释 Ops 发布前注入真 pin；XML/plist 语法验证过 |
| F2 | HIGH | setState after dispose（async gap 无 mounted 检查） | ✅ 已修 |
| F3 | MEDIUM | baseUrl 硬编码无覆盖 | ✅ 已修：--dart-define=API_BASE_URL |
| F4 | MEDIUM | firebase 等死依赖 | ⏳ 记录（删需 pub get 全量验证，建议单独 PR） |
| F5 | LOW | getStatus 死代码 | ⏳ 记录 |
| F6 | LOW | keyCtrl 未 dispose | ✅ 已修：whenComplete dispose |
| F7 | LOW | 非 JSON 200 体误报 "Connection failed" | ✅ 已修：FormatException 单独捕获 |

验证：`flutter analyze` **No issues found** ✓；`flutter test` **4/4 全过**（附带修复：测试缺 secure storage platform mock — channel mock 补齐，既有测试环境缺陷）；XML/plist 语法 python 验证过。

**Electron desktop（首轮盲点，typescript-reviewer）**

| # | 级别 | 问题 | 处置 |
|---|---|---|---|
| E1 | HIGH | restartCount 闭包局部 — 无限重启循环 | ✅ 已修：模块级 + MAX_RESTARTS |
| E2 | MEDIUM | openExternal 任意 http(s) URL | ✅ 已修：仅 https + 日志 |
| E3 | MEDIUM | 无 will-navigate 守卫 — XSS 后 preload 暴露给远程页 | ✅ 已修：仅允许 dev URL / file:// |
| E4 | MEDIUM | openInShell 任意路径 | ✅ 已修：扩展名白名单 |
| E5 | MEDIUM | mode/lang 未编码 + runId 未校验（URL 注入） | ✅ 已修：encodeURIComponent + ^[a-zA-Z0-9-]+$ |
| E6 | MEDIUM | resp.ok 不查 + json 解析异常 → 全部误报 "Backend unreachable" | ✅ 已修：统一 backendFetch |
| E7 | LOW | preload.ts 死代码 | ⏳ 记录（删需验证构建配置引用） |
| E8 | LOW | entrypoint.py 强制 stub provider | ⏳ 记录（打包默认有意为之，建议文档化） |

验证：读码级（本地无 node_modules，tsc 不可跑 — 环境诊断仅为缺 @types/node）。

**P3-45 杂项收尾**

- ✅ mcp/base.py decisions 日志敏感 kwargs 脱敏（key/secret/token/password/credential/auth）— mcp 14/14
- ✅ main.rs "2.0.0" 硬编码 → env!("CARGO_PKG_VERSION")（Cargo.toml 同为 2.0.0，行为不变）
- ✅ .env.bak — 误报，`*.bak` 规则已忽略
- ⏳ llm_cache 永不过期 / Actions Node 20 弃用 — 记录

### QA 第五轮（2026-08-16 09:20~，BMAD 三镜交叉验证轮）

基线：1215/3。CLI soak 10 轮 doctor+catalog 稳定 ✓。BMad 三镜（adversarial 14 / edge-case 8 / verification-gap 4）审 4 个安全核心文件 diff。仲裁：多数发现与前几轮一致（已修）；冲突项按更严结论。

**verification-gap 4 项全修：**
- ✅ 429 顺序：capacity pre-check 移到 submit 前（满负载不再付 LLM 路由+孤儿 DB 行）+ 测试断言 submit 不调用
- ✅ roles isinstance 守卫测试（字符串 "administrator" 不得 ADMIN）— 51/51
- ✅ validate_token_async 测试（生产路径，PyJWTError→401）— 51/51
- ✅ TAGENT_ENTERPRISE_ENABLED mount 子进程测试（完整配置 mounted / 缺配置拒绝启动）

**adversarial/edge-case 真问题 10 项全修：**
- ✅ mode_lang_lock 缩窄到模块全局写入（submit/LLM 路由出锁 — 原实现串行化整个端点）
- ✅ _run_active set → dict[run_id, reserved_at] + 30min TTL 惰性清扫（弃跑不再永久 429）
- ✅ auth_middleware /webhooks/ 前缀豁免（与 enterprise exclude 双层一致 — 原配置下平台回调被外层 401）
- ✅ validate_token_async httpx.HTTPError → 503（IdP 宕机不再 500）
- ✅ _ALLOWED_ALGS 补 EdDSA（Ed25519 IdP 兼容）
- ✅ feishu PKCS#7 pad 校验 + 空 FEISHU_ENCRYPT_KEY fail-closed
- ✅ feishu encrypt+token 混合 payload 必解密验内层
- ✅ DingTalk 重放窗口 5min → 1h（官方容忍 1h，防红发丢失）
- ✅ run_file extra Form 上限 10k + UploadFile 全部路径 close（spool 泄漏）
- ✅ sso decode except 拓宽 (PyJWTError, ValueError)（非 dict payload 不再 500）+ aud list 支持

**冲突项（更严结论保留）：**
- marketplace token 豁免删除 — 保持需 token（安全优先）；edge 镜认为公共目录应开放。记录，待产品决策。
- 未知：企业模式 webhook 401 breaking change 已有文档+日志，保持 fail-closed。

回归：api/enterprise/webhooks 127/127 绿。全量最终跑中。

### QA 第六轮（2026-08-16 10:00~，ruff 战役启动轮）

基线：1219 passed / 4 failed（含 1 个 subprocess 测试全量 flaky → 环境隔离加固；3 已知）。

**P2-29 ruff 根 scope 战役 — 第一阶段完成：**
- 量化：runtime/ + utils/ 共 536 错（456 auto-fixable），集中在 12 文件
- **本轮战役所有改动文件 lint 全绿**（24 文件 0 错）：webhooks.py 19→0（含 E402 bridge import 移顶、B904 ×13 from None、F841）、sso.py 9→0（B904 ×5、SIM102）、rbac、llm_client、v2_prompt（F841）、conversation（E741）、search（SIM105 ×2 + suppress）、kg_api（UP015）、main/enterprise/flows auto-fix
- 剩余 ~490 错在**未触及文件**（interactive.py 20、slash_handlers_* 42、旧测试文件等）— 下批处理，避免一次性 300+ 文件大 diff
- 回归：触及模块 164/164 绿

### QA 第七轮（2026-08-16 10:40~，ruff 战役收官轮）

**P2-29 完成：runtime/ + utils/ ruff 536 → 0 错。**

- 464 auto-fix + 72 手动
- **真 bug 修复（lint 逼出）**：slash_handlers_core `_do_quit` 调未定义的 `_save_session`（/quit 必崩）；interactive.py `_get_prompt_style`/`_repl_print` 未定义（样式构建崩 + 每次 print 静默走 except 兜底）— 全部实现；interactive `_render_rprompt` 算了 pct 不显示（意图恢复）
- **编码修复**：plugin.py、v2_router.py GBK → UTF-8（ruff E902）
- F811×4 import 重定义 → 别名；F821×4 定义补齐/注解修；B904×8、SIM 系列×15、F841×7、E741×3 等
- v2_router 路径检查合并 if（缩进块重排）
- 回归：触及模块 97/97 绿；全量最终跑中

### QA 第八轮（2026-08-16 11:20~，门禁固化轮）

- **CI ruff 门禁扩展**：ci.yml 新增 runtime/ 检查 job（`--config runtime/pyproject.toml`）— 原 CI 只查 utils/ + specs/，根 scope 从未治理
- **双配置排序冲突收口**：根/runtime 两 pyproject 补统一 `[tool.ruff.lint.isort]`（known-first-party），mcp/client.py 函数内 import 块互斥 → 根配置 per-file-ignores（CI 用 runtime 配置）
- **回归抓回一个 auto-fix 误删**：`_PROVIDERS` re-export 被 F401 auto-fix 删除 → 测试 ImportError → 恢复 + 43/43 绿
- CI 等价三门禁验证：utils（utils/.ruff.toml）✓ / runtime（runtime/pyproject.toml）✓ / specs ✓；ci.yml yaml 解析 ✓
- 附：plugin.py、v2_router.py GBK→UTF-8 转换固化

### QA 第九轮（2026-08-16 11:40~，ruff 战役 diff 复审轮）

基线：1219/4 稳定。code-reviewer 审 ruff 战役机械改动 diff（70+ 文件）。

**发现 1 HIGH + 1 MEDIUM（都是我第七轮实现引入）→ 已修：**
- HIGH：`_repl_print` 用 HTML() 不转换 rich markup — `[red]`/`[dim]` 全变字面 tags（整个 REPL 输出损坏；HTML 不抛异常所以兜底永不触发）→ 修复：委托 interactive_ui.repl_print（rich_to_pt 转换 + 降级剥离）
- MEDIUM：`_get_prompt_style` 只读 skin prompt_style 2 键，丢 toolbar/separator 样式 → 修复：委托 interactive_ui.get_prompt_style（colorscheme 全键）
- 12 个文件核验语义等价 ✓（bridge enumerate、v2_router 合并 if 短路顺序、rollback、voice 三元、GBK→UTF-8 无乱码、SIM105 suppress 语义等）

回归：interactive/completer 78/78 绿；ruff 全绿保持。

**杂项：** `.gitignore` 补 Flutter 产物（apps/mobile/.dart_tool/、build/）。工作树 209 文件未提交 — 持续提醒提交中。

### QA 第十轮（2026-08-16 12:20~，收敛轮）

**全维度终核 — 全部通过：**

| 维度 | 结果 |
|---|---|
| Python 全量 | 1219 passed / 4 failed（= 已知稳定态：flaky ping rerun + 无 Chromium e2e ×2）|
| 覆盖率门禁 | ≥30 生效 ✓ |
| ruff runtime/ 门禁 | All checks passed ✓ |
| ruff utils/ 门禁 | All checks passed ✓ |
| selftest L2 实跑 | 9/9 PASS（100%）✓ |
| Flutter analyze | No issues found ✓ |
| Flutter test | 4/4 All tests passed ✓ |

**十轮战役收敛态：** 无未决修复项。剩余队列（非代码项）：
1. **提交** — 209 文件未提交（等用户批准）
2. **S12 权限矩阵** — 端点×权限产品决策（等用户拍板）
3. 弱网/真 LLM/soak — 环境限制 ⏸（CI 周自检 job 覆盖真 LLM）
4. firebase 死依赖清理 — 建议单独 PR

### QA 第十一轮（2026-08-16 13:00~，维持验证轮）

无新代码改动（diff 与第十轮一致）。全量 1219/4 稳定、ruff 双门禁 ✓、覆盖门禁 ✓。状态维持收敛。工作树 209 文件（188 tracked changed + 21 untracked）。CRLF→LF 警告为 ruff 规范化行尾，提交时 git 自动处理，无碍。
| P2-23~40 | ⏳ 待修 | 见问题表 | 逐一修 |
| P3-41~45 | ⏳ 待修 | 见问题表 | 酌情 |

### QA 第十二轮（2026-08-16 13:40~，维持验证轮）

连续第四轮结果一致：全量 1219/4 稳定、ruff 双门禁 ✓、覆盖门禁 ✓、工作树 209 文件无变化。状态持续收敛。无新发现、无新改动。

### QA 第十三轮（2026-08-16 14:20~，维持验证轮）

连续第五轮一致：全量 1219/4、ruff 双门禁 ✓、覆盖 ✓、工作树 209 无变化。无新发现。

### QA 第十四轮（2026-08-16 15:00~，提交后首轮）

工作树干净（0 未提交）。干净树上全量 1219/4、ruff 双门禁 ✓、覆盖 ✓。3 提交（aaeb7d8/372dd5e/c8c896f）已推送远程分支 fix/mcp-pin-2.0.0。状态持续收敛。

### S12 权限矩阵完成（2026-08-16 15:40，用户拍板：参照 GitHub 仓库权限模型）

- 矩阵：viewer 只读 / tester 发起+取消 / manager 管理 / admin 全权（health/docs/metrics/webhooks 豁免）
- @rbac.require 挂 14 端点；三层语义（RBAC 禁用放行 / 无中间件放行 / 有角色按矩阵）
- sync+async 双支持；端点补 request 参数；TestRbacMatrix 4/4
- 全量 1223/4（+4）；提交 2f3d807 已推送

**十二轮战役全部待办清零。**
