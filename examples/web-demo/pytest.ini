[pytest]
testpaths = tests

markers =
    p0: P0 级核心用例
    smoke: 冒烟用例
    ui: UI 测试

addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings

timeout = 60
log_cli = true
log_cli_level = INFO
