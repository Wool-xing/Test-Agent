#!/usr/bin/env bash
# 给 OBS / QuickTime / 系统屏幕录制用 · 你按录制键 → 跑此脚本 → 节奏自动调好
#
# 用法:
#   1. 打开 OBS / 系统录屏,选好窗口
#   2. 摁开始录制
#   3. 在终端跑:bash scripts/record-demo-obs.sh
#   4. 跑完(~30s 含暖场)摁停止录制
#
# 输出:无文件(屏幕录制由 OBS 自己存)。本脚本只控制终端节奏。

set -e

# 暖场 3 秒,给你时间切到录制窗口 + 调好取景
echo "──────────────────────────────────────────"
echo "  Test-Agent V1.14 · 30 秒 demo 录制"
echo "──────────────────────────────────────────"
echo ""
echo "请确认:"
echo "  □ OBS / 屏幕录制已摁开始"
echo "  □ 终端字号 ≥ 16pt"
echo "  □ 终端窗口 ≥ 100 列 × 28 行"
echo ""
read -p "按回车开始(3 秒后跑命令)..."
sleep 3

clear
bash scripts/_demo-commands.sh

echo ""
echo "──────────────────────────────────────────"
echo "✓ demo 完成 · 请摁 OBS 停止录制"
echo "──────────────────────────────────────────"
