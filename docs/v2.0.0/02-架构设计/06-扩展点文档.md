# 扩展点文档 — Plugin / MCP / Skill

> 日期: 2026-06-21
> 协议: §四-A 原则3 一切皆Plugin/Skill

## Skill 扩展点

新增Skill: 写1个 SKILL.md + 可选 executor.py

```
skills/my-check/
├── SKILL.md          # YAML frontmatter (name/version/permissions) + Markdown body
├── executor.py       # (可选) execute(params, ctx) -> SkillResult
└── test_my_check.py  # 自带测试
```

注册: SDK自动发现 `skills/` 目录下的 SKILL.md

## LLM Provider 扩展点

新增Provider: 1个文件 + 注册

```python
# runtime/agent/llm/my_provider.py
from runtime.agent.llm.base import BaseLLMProvider, register

@register("my-provider")
class MyProvider(BaseLLMProvider):
    async def chat(self, messages, **kwargs): ...
    async def stream(self, messages, **kwargs): ...
```

## 报告格式 扩展点

新增格式: 实现 ReportFormatter 接口

```python
# runtime/exporters/my_format.py
from runtime.exporters.base import BaseFormatter

class MyFormatter(BaseFormatter):
    def format(self, results: list[TestResult]) -> str: ...
    def content_type(self) -> str: ...
```

## 通知渠道 扩展点

新增通知: 实现 Notifier

```python
# runtime/gateway/platforms/my_channel.py
from runtime.gateway.base import BaseNotifier

class MyNotifier(BaseNotifier):
    async def send(self, message: str, target: str): ...
```

## MCP Server 集成

```json
// .mcp.json
{
  "mcpServers": {
    "my-server": {
      "command": "python", "args": ["-m", "my_mcp_server"],
      "env": {"API_KEY": "${MY_API_KEY}"}
    }
  }
}
```
工具自动发现: `tagent catalog` → MCP tools listed

## 配置扩展点

tagent.yaml 中新增配置项: 代码中读取 `settings.my_new_option`

---
*文档创建: 2026-06-21 | Sprint 3 补*
